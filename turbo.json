{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "env": ["DATABASE_URL","DIRECT_URL","ENV"]
    },
     "db:push": {
      "env": ["DATABASE_URL","DIRECT_URL","ENV"],
      "cache": false
    }
  }
}
