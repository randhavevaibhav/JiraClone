{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "env": ["DATABASE_URL","DIRECT_URL","ENV"]
    },
     "dev": {
      "cache": false,
      "persistent": true
    },
     "db:push": {
      "env": ["DATABASE_URL","DIRECT_URL","ENV"],
      "cache": false
    }
  }
}
